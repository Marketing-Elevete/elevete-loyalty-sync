
:root{--cream:#F8EFE1;--cream-2:#F1E7D5;--ink:#141210;--ink-soft:#5b544c;--purple:#4A2D8F;--orange:#E8622A;
--gap:16px;--pad:20px;--maxw:1120px;--display:"Dela Gothic One",sans-serif;--body:"DM Sans",sans-serif;--lock:#b8b0a4}
*{box-sizing:border-box;margin:0;padding:0}
body{background:var(--cream);color:var(--ink);font-family:var(--body);line-height:1.5;-webkit-font-smoothing:antialiased;padding-bottom:76px}
@media(min-width:900px){body{padding-bottom:0}}
img{max-width:100%;display:block}
.wrap{max-width:var(--maxw);margin:0 auto;padding:0 var(--pad)}
h1,h2,h3{font-family:var(--display);font-weight:400;line-height:1.05;letter-spacing:-0.01em}
.eyebrow{font-family:var(--body);font-weight:700;font-size:12px;letter-spacing:.14em;text-transform:uppercase;color:var(--orange)}

/* view toggle */
.vtoggle{position:fixed;top:10px;right:10px;z-index:80;display:flex;gap:0;background:var(--ink);padding:3px;font-size:11px;font-weight:700}
.vtoggle button{border:0;background:transparent;color:#fff;padding:6px 12px;cursor:pointer;font-family:var(--body);font-weight:700;font-size:11px}
.vtoggle button.on{background:var(--orange)}

main{display:flex;flex-direction:column}
section{order:5}

/* ---- member hero ---- */
.mhero{padding:52px 0 44px;text-align:center;background:radial-gradient(120% 100% at 50% 0%,#fff 0%,var(--cream) 62%)}
.mhero__hi{font-family:var(--display);font-size:clamp(1.4rem,6vw,2rem);margin:10px 0 16px}
.mhero__bal{display:inline-flex;flex-direction:column;align-items:center;margin-bottom:30px}
.mhero__num{font-family:var(--display);font-size:clamp(3.4rem,18vw,5.5rem);line-height:.9;color:var(--orange)}
.mhero__unit{font-weight:700;font-size:13px;letter-spacing:.12em;text-transform:uppercase;color:var(--ink-soft);margin-top:4px}
.track{max-width:560px;margin:0 auto}
.track__head{display:flex;flex-direction:column;gap:4px;font-size:14px;margin-bottom:34px}
.track__head b{color:var(--purple)} .track__goal{color:var(--ink-soft)} .track__goal b{color:var(--orange)}
.track__bar{position:relative;height:6px;background:rgba(74,45,143,.15);margin:0 8px 52px}
.track__fill{position:absolute;top:0;left:0;height:100%;background:var(--orange)}
.track__stop{position:absolute;top:50%;transform:translate(-50%,-50%);text-align:center}
.track__stop i{display:block;width:16px;height:16px;border-radius:50%;background:#fff;border:3px solid rgba(74,45,143,.3);margin:0 auto}
.track__stop--done i{background:var(--orange);border-color:var(--orange)}
.track__stop em{position:absolute;top:22px;left:50%;transform:translateX(-50%);font-style:normal;font-size:11px;font-weight:700;line-height:1.2;white-space:nowrap;color:var(--ink-soft)}
.track__stop--done em{color:var(--purple)}
.track__stop--first em{left:0;transform:none;text-align:left}
.track__stop--last em{left:auto;right:0;transform:none;text-align:right}
.track__note{font-size:12px;color:var(--ink-soft);opacity:.8;margin-top:6px}
.track__unlock{display:inline-block;margin-top:4px;font-size:13px;font-weight:700;color:var(--purple)}
.track__unlock b{color:var(--orange)}

/* ---- guest hero ---- */
.ghero{padding:56px 0 44px;text-align:center}
.ghero h1{font-size:clamp(2.1rem,9vw,3.4rem);margin:14px auto 0;max-width:14ch}
.ghero h1 .hl{color:var(--purple)}
.ghero__sub{font-size:16px;color:var(--ink-soft);max-width:34ch;margin:16px auto 0}
.ghero__pill{display:inline-block;margin-top:22px;background:var(--purple);color:#fff;font-weight:700;font-size:14px;padding:8px 16px}
.ghero__pill b{color:#FCD34D}

.cta-row{display:flex;flex-direction:column;gap:10px;margin-top:24px;align-items:center}
.btn{display:inline-flex;align-items:center;justify-content:center;font-family:var(--body);font-weight:700;font-size:15px;padding:15px 30px;border:0;cursor:pointer;text-decoration:none;transition:transform .12s cubic-bezier(.23,1,.32,1);width:100%;max-width:320px}
.btn:active{transform:scale(.97)}
.btn--primary{background:var(--orange);color:#fff} .btn--ghost{background:transparent;color:var(--ink);border:1.5px solid var(--ink)}

/* ---- sections + boundaries ---- */
.section{padding:48px 0;position:relative}
.section+.section{border-top:1px solid rgba(20,18,16,.10)}
.section--tint{background:var(--cream-2)}
.section__head{text-align:center;margin-bottom:26px}
.section__head h2{font-size:clamp(1.5rem,6vw,2.2rem);margin-top:8px}
.section__head p{color:var(--ink-soft);font-size:15px;margin-top:8px;max-width:42ch;margin-inline:auto}

/* how it works */
.steps{display:flex;flex-direction:column;gap:14px;max-width:520px;margin:0 auto}
.step{display:flex;gap:16px;align-items:flex-start;background:var(--cream);padding:18px 20px}
.section--tint .step{background:#fff}
.step__n{font-family:var(--display);font-size:22px;color:var(--orange);line-height:1}
.step__t{font-family:var(--display);font-size:15px;margin-bottom:4px} .step__d{font-size:14px;color:var(--ink-soft)}

/* earn: featured + tiles */
.earn-feat{display:flex;align-items:center;gap:16px;background:var(--purple);color:#fff;padding:22px 24px;margin-bottom:var(--gap)}
.earn-feat__icon{font-size:34px}
.earn-feat__t{font-family:var(--display);font-size:18px}
.earn-feat__d{font-size:13px;opacity:.85;margin-top:2px}
.earn-feat__pts{margin-left:auto;text-align:right;font-family:var(--display);font-size:22px;color:#FCD34D;white-space:nowrap}
.earn-feat__pts small{display:block;font-family:var(--body);font-size:11px;font-weight:700;opacity:.85;color:#fff}
.tiles{display:grid;grid-template-columns:repeat(2,1fr);gap:var(--gap)}
.tile{background:#fff;padding:18px 12px;display:flex;flex-direction:column;align-items:center;text-align:center;gap:6px}
.section:not(.section--tint) .tile{background:var(--cream-2)}
.tile__icon{font-size:24px} .tile__label{font-weight:500;font-size:13px} .tile__pts{font-weight:700;font-size:12px;color:var(--purple)}

/* rewards */
.subhead{font-family:var(--display);font-size:15px;margin:0 0 14px;display:flex;align-items:center;gap:10px}
.subhead::after{content:"";flex:1;height:1px;background:rgba(20,18,16,.14)}
.afford-note{text-align:center;font-size:13px;color:var(--ink-soft);margin:-8px 0 24px}
.afford-note b{color:var(--orange)}
.vgrid{display:grid;grid-template-columns:repeat(3,1fr);gap:var(--gap);margin-bottom:34px}
.vcard{background:var(--purple);color:#fff;padding:18px 10px;text-align:center;display:flex;flex-direction:column;gap:2px}
.vcard__amt{font-family:var(--display);font-size:clamp(1.1rem,5vw,1.5rem)} .vcard__sub{font-size:11px;opacity:.8}
.vcard__pts{margin-top:6px;font-weight:700;font-size:12px;color:#FCD34D}
.cards{display:grid;grid-template-columns:repeat(2,1fr);gap:var(--gap)}
.card{background:#fff;overflow:hidden;display:flex;flex-direction:column}
.card__img{position:relative;aspect-ratio:1/1;overflow:hidden;background:var(--cream-2)}
.card__img img{width:100%;height:100%;object-fit:cover}
.card__tier{position:absolute;top:8px;left:8px;font-size:10px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;padding:4px 8px;color:#fff}
.card__tier--premium{background:var(--purple)} .card__tier--standard{background:var(--orange)}
.card__body{padding:12px;display:flex;flex-direction:column;flex:1}
.card__title{font-family:var(--body);font-weight:700;font-size:14px;line-height:1.25;min-height:2.5em}
.card__pts{margin-top:6px;font-weight:700;font-size:12px;color:var(--purple)}
/* affordability states */
.rc__go{margin-top:10px;display:inline-block;font-weight:700;font-size:12px;padding:8px 0;text-align:center;color:var(--lock)}
.rc__go--ok{background:var(--orange);color:#fff;padding:9px 14px}
.card .rc__go{margin-top:auto}
.vcard .rc__go{margin-top:8px} .vcard .rc__go{color:rgba(255,255,255,.6)} .vcard .rc__go--ok{color:#fff}
/* lock visuals ONLY apply in member view (guest sees full aspirational catalog) */
body.v-member .card.is-lock .card__img img{filter:grayscale(.35) opacity(.72)}
body.v-member .card.is-lock,body.v-member .vcard.is-lock{opacity:.82}

/* earn tile states */
.tile__state{margin-top:8px;font-size:11px;font-weight:700;letter-spacing:.03em}
.tile__state--done{color:#2f7d43} .tile__state--claim{color:var(--orange)}
body.v-member .tile.is-done{opacity:.72}
/* quest progress */
.quest{max-width:520px;margin:0 auto 22px;text-align:center}
.quest__label{font-size:13px;font-weight:700;color:var(--ink-soft);margin-bottom:8px}
.quest__label b{color:var(--orange)}
.quest__bar{height:6px;background:rgba(74,45,143,.15)} .quest__fill{height:100%;background:var(--orange)}
/* next-reward nudge */
.nudge{position:relative;overflow:hidden;display:flex;align-items:center;gap:14px;background:#fff;border:1.5px solid var(--orange);padding:14px 18px;margin:0 0 24px}
.section--tint .nudge{background:var(--cream)}
.nudge__icon{font-size:26px} .nudge__t{font-size:14px;margin-bottom:8px} .nudge__t b{color:var(--orange)}
.nudge__bar{height:5px;background:rgba(232,98,42,.2);overflow:hidden} .nudge__fill{height:100%;background:var(--orange)}
.nudge__body{flex:1}
/* milestone celebration — CSS-only shine sweep (GPU: transform/opacity only, no layout thrash) */
.nudge::after{content:"";position:absolute;top:0;left:0;width:60%;height:100%;pointer-events:none;
  background:linear-gradient(100deg,transparent 0%,rgba(255,255,255,.55) 50%,transparent 100%);
  transform:translateX(-160%);animation:shine 3.6s ease-in-out infinite}
.nudge__fill{animation:fillpulse 3.6s ease-in-out infinite}
@keyframes shine{0%,100%{transform:translateX(-160%)}55%,80%{transform:translateX(260%)}}
@keyframes fillpulse{0%,100%{opacity:1}50%{opacity:.78}}
@media (prefers-reduced-motion: reduce){.nudge::after{display:none} .nudge__fill{animation:none}}
/* guest 'join to redeem' cta */
.guest-cta{text-align:center;margin:8px 0 26px}
.guest-cta a{display:inline-block;background:var(--orange);color:#fff;font-weight:700;font-size:14px;padding:13px 26px;text-decoration:none}

/* coming soon teasers */
.soon-grid{display:grid;grid-template-columns:1fr;gap:var(--gap)}
.soon{position:relative;background:#fff;border:1.5px dashed rgba(74,45,143,.35);padding:24px 20px;text-align:center}
.soon__badge{position:absolute;top:14px;right:14px;background:var(--purple);color:#fff;font-size:10px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;padding:4px 8px}
.soon__icon{font-size:34px;display:block;margin-bottom:8px;filter:grayscale(.15)}
.soon__title{font-family:var(--display);font-size:16px;color:var(--purple);margin-bottom:4px}
.soon__note{font-size:13px;color:var(--ink-soft)}
@media(min-width:900px){.soon-grid{grid-template-columns:repeat(3,1fr)}}

/* gratitude band */
.thanks{text-align:center;max-width:640px;margin:0 auto}
.thanks__t{font-size:clamp(1.5rem,6vw,2.2rem);margin-bottom:14px}
.thanks__p{font-size:15px;color:var(--ink-soft);line-height:1.6}

/* scroll-reveal (progressive enhancement: visible by default, JS adds .reveal for the animated state) */
.js .reveal{opacity:0;transform:translateY(16px)}
.js .reveal.in{opacity:1;transform:none;transition:opacity .5s cubic-bezier(.23,1,.32,1),transform .5s cubic-bezier(.23,1,.32,1)}
@media (prefers-reduced-motion: reduce){.js .reveal{opacity:1;transform:none;transition:none}}
/* confetti canvas */
#confetti{position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:200}

/* tiers */
.tiers{display:flex;flex-direction:column;gap:var(--gap)}
.tiercol{background:#fff;padding:22px;position:relative;border-top:3px solid var(--ink)}
.section:not(.section--tint) .tiercol{background:var(--cream-2)}
.tiercol--current{border-top-color:var(--orange);box-shadow:0 0 0 2px var(--orange)}
.tiercol__num{font-family:var(--display);font-size:14px;color:var(--ink-soft)}
.tiercol__name{font-size:22px;margin:6px 0 2px;color:var(--purple)} .tiercol__tag{font-size:13px;color:var(--ink-soft)}
.tiercol__rate{margin-top:14px;font-size:15px} .tiercol__rate b{font-family:var(--display);font-size:22px;color:var(--orange)}
.tiercol__you{position:absolute;top:16px;right:16px;background:var(--orange);color:#fff;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.06em;padding:4px 8px}
.tiercol__perks{list-style:none;margin:16px 0 0;padding:16px 0 0;border-top:1px solid rgba(20,18,16,.12)}
.tiercol__perks li{font-size:13px;color:var(--ink-soft);padding:5px 0 5px 20px;position:relative}
.tiercol__perks li::before{content:"";position:absolute;left:0;top:9px;width:9px;height:6px;border-left:2px solid var(--orange);border-bottom:2px solid var(--orange);transform:rotate(-45deg)}

/* tier comparison matrix */
.mx-scroll{overflow-x:auto;-webkit-overflow-scrolling:touch}
.mx{display:grid;grid-template-columns:1.4fr 1fr 1fr 1fr;min-width:340px;background:rgba(20,18,16,.08);gap:1px;border:1px solid rgba(20,18,16,.08)}
.mx__cell{background:var(--cream);padding:12px 10px;display:flex;flex-direction:column;justify-content:center;font-size:13px}
.mx__lbl{color:var(--ink);font-weight:500;text-align:left}
.mx__val{align-items:center;text-align:center}
.mx .mx__val.is-cur{background:#fff}
.mx__head{align-items:center;text-align:center;position:relative;padding-top:22px;background:var(--cream-2)}
.mx .mx__head.is-cur{background:var(--purple)}
.mx .mx__head.is-cur .mx__tname{color:#fff}
.mx__tname{font-family:var(--display);font-size:15px;color:var(--purple)}
.mx__head.is-cur .mx__tname{color:#fff}
.mx__how{font-size:11px;color:var(--ink-soft);margin-top:2px} .mx__head.is-cur .mx__how{color:rgba(255,255,255,.85)}
.mx__here{position:absolute;top:0;left:0;right:0;background:var(--orange);color:#fff;font-size:9px;font-weight:700;letter-spacing:.05em;text-transform:uppercase;padding:3px}
.mx__rate{font-family:var(--display);font-size:20px;color:var(--orange)}
.mx__corner{background:transparent;border:0}
.mx__check{color:#2f7d43;font-size:17px;font-weight:700;display:inline-block}
.mx__no{color:var(--lock);font-size:16px}
.mx-note{text-align:center;font-size:13px;color:var(--ink-soft);margin-top:16px}
/* checkmark stagger-in when matrix scrolls into view */
.js .mx__check{opacity:0;transform:scale(.3)}
.js .mx.in .mx__check{opacity:1;transform:none;transition:opacity .3s ease-out,transform .35s cubic-bezier(.34,1.56,.64,1);transition-delay:var(--d)}
@media (prefers-reduced-motion: reduce){.js .mx__check{opacity:1;transform:none;transition:none}}

/* faq */
.faq{max-width:640px;margin:0 auto}
.faq__item{border-bottom:1px solid rgba(20,18,16,.14)}
.faq__item summary{font-family:var(--display);font-size:15px;padding:18px 0;cursor:pointer;list-style:none;display:flex;justify-content:space-between;gap:16px}
.faq__item summary::-webkit-details-marker{display:none} .faq__item summary::after{content:"+";color:var(--orange);font-size:20px}
.faq__item[open] summary::after{content:"–"} .faq__item p{padding:0 0 18px;color:var(--ink-soft);font-size:14px;max-width:60ch}

/* sticky */
.sticky{position:fixed;left:0;right:0;bottom:0;background:var(--ink);color:#fff;padding:12px 16px;display:flex;align-items:center;justify-content:space-between;gap:12px;z-index:50}
.sticky__txt{font-weight:500;font-size:13px} .sticky__txt b{font-family:var(--display);color:#FCD34D}
.sticky .btn{width:auto;padding:12px 22px;font-size:14px}

/* ===== VIEW SWITCHING + ORDERING ===== */
body.v-guest .only-member{display:none}
body.v-member .only-guest{display:none}
/* member reading order: hero > redeem > tiers > earn > coming-soon > thanks > faq */
body.v-member .s-mhero{order:1} body.v-member .s-treats{order:2} body.v-member .s-tiers{order:3}
body.v-member .s-earn{order:4} body.v-member .s-soon{order:5} body.v-member .s-thanks{order:6} body.v-member .s-faq{order:7}
body.v-member .sticky-txt-guest{display:none}
/* guest reading order: hero > how > earn > treats > tiers > coming-soon > thanks > faq */
body.v-guest .s-ghero{order:1} body.v-guest .s-how{order:2} body.v-guest .s-earn{order:3}
body.v-guest .s-treats{order:4} body.v-guest .s-tiers{order:5} body.v-guest .s-soon{order:6} body.v-guest .s-thanks{order:7} body.v-guest .s-faq{order:8}
body.v-guest .sticky-txt-member{display:none}
@media(min-width:900px){
  .cta-row{flex-direction:row;justify-content:center} .btn{width:auto}
  .steps{flex-direction:row;max-width:none} .step{flex:1;flex-direction:column}
  .tiles{grid-template-columns:repeat(4,1fr)} .cards{grid-template-columns:repeat(4,1fr)}
  .tiers{flex-direction:row} .tiercol{flex:1} .section{padding:64px 0} .sticky{display:none}
}
